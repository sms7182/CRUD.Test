# CRUD.Test:
this project a simple crud test with authorization
# requirements:
  . using postgresdb
  . using microsoft identity
  . env: dockerize
# commands for run: 
  docker-compose build
  docker-compose up 
#Login:
  user:test-user
  pass:test1234
